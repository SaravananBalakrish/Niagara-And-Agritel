import 'package:flutter/material.dart';

class WaterPumpSettings extends StatelessWidget {
  const WaterPumpSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
